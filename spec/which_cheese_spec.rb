require 'spec_helper'

describe WhichCheese do
  it 'has a version number' do
    expect(WhichCheese::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
